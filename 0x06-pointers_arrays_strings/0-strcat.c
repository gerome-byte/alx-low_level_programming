#include "main.h"

/**
 * char *_strcat - concatenates two strings
 * @str1[] - first string
 * @str2[] - second string
 * ptr - pointer
 * return 0
 */

void char *_strcat(char *dest, char *src)

{
	char str1[];
	char str2[];
	char *ptr;

	ptr = _strcat(str1, str2);

	return = (0);
}
